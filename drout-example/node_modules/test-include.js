
const Drout = require("drout");

class TestInclude extends Drout {
  static render(options) {
    return `success: ${options.what}`;
  }
  static getDefault() {
    return {
      what: "now",
      how: "now",
      who: "now",
    };
  }
}

module.exports = TestInclude;
